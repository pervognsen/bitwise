#static_assert(sizeof(long) == 4)
#static_assert(sizeof(ulong) == 4)

@foreign
const LONG_MIN = long(INT32_MIN);

@foreign
const LONG_MAX = long(INT32_MAX);

const ULONG_MIN = ulong(INT32_MIN);

@foreign
const ULONG_MAX = ulong(INT32_MAX);
