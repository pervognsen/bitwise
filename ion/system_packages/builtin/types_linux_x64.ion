#static_assert(sizeof(long) == 8)
#static_assert(sizeof(ulong) == 8)

@foreign
const LONG_MIN = long(INT64_MIN);

@foreign
const LONG_MAX = long(INT64_MAX);

const ULONG_MIN = ulong(INT64_MIN);

@foreign
const ULONG_MAX = ulong(INT64_MAX);
