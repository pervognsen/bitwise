@foreign("wchar_t")
typedef wchar = uint32;

@foreign
const WCHAR_MIN = UINT32_MIN;

@foreign
const WCHAR_MAX = UINT32_MAX;

@foreign
typedef time_t = long;

@foreign
typedef clock_t = long;

@foreign
const CLOCKS_PER_SEC = clock_t(1000000);
